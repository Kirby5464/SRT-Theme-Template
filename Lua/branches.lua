local branches = {}

function branches.Evaluation(song)
    --[[if string.find(song:GetSongDir(), "#AK47") then
        return "ScreenEvaluationSTG5"
    end]]
    return "ScreenEvaluationStage"
end

return branches